{
  "name": "@wa-chat/api",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "typecheck": "npm --workspace @wa-chat/config run build && npm --workspace @wa-chat/shared run build && npm --workspace @wa-chat/llm run build && tsc --noEmit",
    "test": "npm --workspace @wa-chat/config run build && npm --workspace @wa-chat/shared run build && npm --workspace @wa-chat/llm run build && node --import tsx src/index.test.ts",
    "start": "node dist/index.js",
    "build": "npm --workspace @wa-chat/config run build && npm --workspace @wa-chat/shared run build && tsc"
  },
  "dependencies": {
    "@wa-chat/config": "*",
    "@wa-chat/llm": "*",
    "@wa-chat/shared": "*",
    "bullmq": "^5.58.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "redis": "^5.8.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.3",
    "tsx": "^4.19.4"
  }
}
